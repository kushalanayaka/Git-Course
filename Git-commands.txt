Git --version
git config global user.name "Kushal" ( To set)
git config global user.email "Kushal@g.c" ( To set)

git config global user.name  ( To get)
git config global user.email  ( To get)

git config --list (to get both)

git clone "https://github.com/kushalanayaka/Git-Course.git"

cd.\Git-course\ (enter tab to auto name suggestion )

git status

git add "file name"
git add . ( to add all files)

git commit -m "Comment"

git remote add origin <-link->

git remote -v (to verify remote)

git branch (to check branch)

git branch -M main ( to rename branch)

git push origin main 

git push -u origin main ( -u to set upstream, for upcoming commands not write origin main again) 


keep both your previous commits and the existing files on GitHub.

git fetch origin

git pull origin main --rebase